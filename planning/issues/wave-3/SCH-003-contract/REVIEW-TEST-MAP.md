# SCH-003 / ORDER 联合契约审阅与测试映射

状态：**REVIEW CANDIDATE / ALL TESTS NOT_EXECUTED**。对应 [CCR 候选](../../../ccr/CCR-W2-API-001/reservation-order-protection-proposal.md)，须在 ROC-1～6 经人工批准和各 Owner 同步后转为实施门禁。它不宣称本分支有生产代码、DDL 或测试结果。

## 审阅时须逐项定稿

| 决定 | Owner/需同步的面 | 通过条件 |
|---|---|---|
| ROC-1 全店精确可行性 | SCH-003 + SCH-004 + ORDER/MER，06/07/10/11/34 | 同一员工跨服务不重复占用；已指派固定、未指派只作内部匹配；完整无解 409、事实或求解故障 503；多连接下与减员/指派串行。 |
| ROC-2 接送单人双段 | 产品 + SCH/ORDER/商家端，06/07/10/11 | 明确一名最终员工须覆盖 PICKUP、RETURN 各自完整窗；页面不显示技术匹配人；不增加两人最终指派字段。若不批准须重做模型。 |
| ROC-3 到店 GENERAL | 产品 + SCH/C 端，06/07/10/11/34 | **新增产品选择**：新单唯一原窗完整容纳；跨相邻窗的用户可见失败被明确批准或另立多 claim 契约；既有服务时长决定实际预约区间的规则不变，存量跨窗不伪造。 |
| ROC-4 hold/order 绑定 | ORDER + SCH + TX/优惠券/支付，06/07/10/11/23 | `order_id NOT NULL`；预分配 ID；hold 与主单同库顶层事务双向绑定；外部支付在提交后，故障补偿可重入；幂等回执原子。 |
| ROC-5 当前指派完整性 | ORDER + MER + SCH，06/07/34 | 按全店/目标员工包含活跃 claim 以外的当前指派；两份 ORDER 当前事实一致；生命周期真值表与不变量故障可证明，索引 EXPLAIN 有证据。 |
| ROC-6 改期保留指派 | 产品 + ORDER/SCH/C/商家端，07/10/11 | **新增产品选择**：原指派不能覆盖新时间时，409 且原预约、指派、改期次数、确认周期均不变；未批准前不把此分支当既定规则。 |

若产品选择自动撤派，须另行批准页面、审计和 ORDER 状态转移。现行 07 的 `ScheduleCommandApi.assignStaff` 与 ORDER 双写冲突须归并到 ORDER 权威，不让 SCH 直接维护 ORDER 当前指派。保护真值表只使用正式 `OrderStage`、`VerificationStatus`、`AppointmentReservationStatus` 与可选的 REFUND 权威 `RefundStatus`，不以展示态或新造核销枚举作依据。

## 必须执行的行为证据（当前均未执行）

| ID | 场景和输入 | 期望结果 |
|---|---|---|
| P01 | IN_STORE 一条 GENERAL 窗完整覆盖 09:10–10:40，service duration 与请求一致，甲全程合格且配置容量 1 | 一 GENERAL claim 原窗 ID 与实际区间，hold→order 一次提交；重放同 key 回首次结果。 |
| P02 | 接送 PICKUP 10:00–11:00、RETURN 12:00–13:00，同店同服务两所选窗，甲分别有班；中间 11:00–12:00 无班 | 两完整 claim 同事务创建，甲可行；只检查开始 +120，不要求中间连续排班。 |
| P03 | 接送前段仅甲、后段仅乙 | 同人语义获批后 409，无 reservation/order/claim/幂等成功回执。 |
| P04 | 甲同时有 A/B 能力，A 已锁 10:00–11:00，B 同段空窗且配置容量 1 | B 单服务窗口人数仍为 1，但全店求解无解，B hold 409；甲不重复占用。 |
| P05 | 未指派 X 可甲/乙，新 Y 仅甲且与 X 相交 | 完整求解把 X/乙、Y/甲作为可行证据；ORDER `service_staff_id` 对 X/Y 仍 NULL。 |
| P06 | IN_STORE 同一 GENERAL 原窗内两不相交实际服务区间；两 claim 半开边界恰好相邻 | 可共用同一员工与配置容量，按实际区间计，不把整个 GENERAL 窗误当两次全窗占用。 |
| P07 | 原窗结束或员工排班结束恰等于下笔开始 | 半开边界可衔接；跨天 claim 不切成按天的新窗。 |
| P08 | 到店请求跨两个相邻 GENERAL 窗、无唯一容纳窗；另测历史重叠造成两窗可包容 | 候选方案下新单失败关闭，不凭开始点/第一条选窗，不留下半个 claim；上线文案与产品裁决一致。 |
| P09 | 窗口 ID 与店/服务/kind 不符，接送 PICKUP/RETURN ID 重复，或两段只有一段可用 | 请求失败且两个 claim 均不落库。 |
| P10 | TEMP_LOCKED `lock_expire_at` 已过但 EXPIRED 尚未提交；另一订单同时 hold | 仍计占用直到过期事务提交；失败或重试不借过期时间抢先复用。 |
| P11 | 商家将订单指派给乙；乙对两段均合格但与另一固定订单重叠 | 指派 409；`pet_order.service_staff_id` 与当前 assignment 两者均维持旧值/NULL，无半次改派。 |
| P12 | 商家已指派甲后减少甲能力/排班；乙仍可覆盖全部预约 | 减员 409，须先合法改派甲的未完成订单；不能用总人数未变放行。 |
| P13 | 用户改期的新窗容得下预约、原指派甲无法覆盖 | **ROC-6 获批后** 409 `SCHEDULE_SWAP_FAILED`，原预约/两 claim/指派/改期次数/确认截止时间不变；未批准前该分支不启用。 |
| P14 | 退款申请、退款单已创建、退款处理中；然后渠道退款成功但 SCH release 消费滞后 | release 前 claim 仍占用并固定当前指派；最终 release 幂等后才解除。迟到支付不恢复订单/预约。 |
| P15 | COMPLETED/VERIFIED 历史指派；CANCELED + RELEASED 历史指派 | 全店查询仍枚举且核验两份当前事实，但 `protectRequired=false`；不把历史员工当未来业务指派。 |
| P16 | PENDING_SERVICE + RELEASED、两份当前指派不一致、reservation 无订单、旧接送 GENERAL 不可恢复 | 503、减员/新 hold 整笔回滚并告警；不能当空事实或 409 普通容量不足。 |
| P17 | 订单创建在 hold 后失败；同事务券冻结失败；提交 ACK 未知后同 requestId 重试 | 前两者整个本地事务回滚；未知 ACK 查原键，若已提交只回首次 orderId，不重复 hold 或订单。 |
| P18 | 主单与 hold 已提交，随后外部支付创建失败 | 使用原创建/补偿幂等链关闭并释放临时占用；不在已提交状态下谎称订单不存在。 |
| P19 | 两个不同 requestId 同时抢一个容量；同 key 同参并发/同 key 异参 | 有界锁竞争下仅一个新占用成功；同参回首次回执，异参 `IDEMPOTENCY_KEY_CONFLICT`。 |
| P20 | 排班缩短与 hold、能力撤销与 ORDER 指派、ORDER 改派与 MER 停用三组双连接屏障测试，交换先后提交顺序 | 每组只有序列化后仍合法的一方可成功；无双方按旧事实都成功。锁顺序为幂等 → 同店 guard → owner 行，死锁重试复核原键。 |
| P21 | 全店当前指派分页、目标员工过滤、已 RELEASED 但 PENDING_SERVICE、及空列表 | 完整性校验发现不一致并 503；空列表须证明真实完整；不可只用活跃 reservationIds 反查。 |
| P22 | 历史 IN_STORE GENERAL、接送旧 GENERAL、存量跨窗、头/claim 缺失数据盘点和回滚演练 | 可恢复数据经前后集合/数量对账；不可恢复按店/服务隔离，旧订单继续履约，不双向复制旧 GENERAL。 |
| P23 | 大版本 ID 字符串、分钟/毫秒边界、非零亚毫秒、未知 kind/状态、跨店请求 | 07/10/11/12 与 HTTP 适配器一致；ID 无 JS 精度损失；非法/未知数据失败关闭。 |
| P24 | 实际门店规模与最坏共享能力图的求解、SQL `EXPLAIN`、性能预算超时 | 找解结果与穷举基准相同；预算耗尽返回 503 且零写入；不以未经批准的预约上限或贪心替代。 |

测试应使用真实 MySQL/InnoDB 多连接与提交屏障；纯 mock、单线程内存模拟、Redis 原子锁或仅 Controller 断言不能证明共同事务与跨域一致性。真实性能预算、隔离级别、索引和回滚迁移版本在实施 PR 报告，未获批准前不把表中场景写成“已通过”。

## 建议实施顺序

1. ORDER/MER 在同店 guard 下实现当前事实 API、状态判定与全量一致性检查；先把读失败和不变量损坏稳定映射为 503。
2. SCH 实现 claim/原窗当前读和完整求解，保护窗口、排班与能力写；与 ORDER 指派/改派在真实主库并发验证。
3. ORDER/TX 联合实现预生成 orderId、hold/create 同事务和提交后支付补偿；客户端仍走原 requestId。
4. SCH-003 接到店唯一 GENERAL 与已批双方向选窗，交 hold/confirm/swap/release；同步 06/07/10/11/12、前端和存量迁移。
5. 上线前核对 P01～P24，披露仍隔离的旧数据与未验证性能，按开关逐店开放；缺少对应证据时相关入口维持已批失败关闭。
