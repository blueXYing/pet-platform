## 宠物档案页与统一 C 端底栏

C-002 宠物页阶段：列表入口（78:2817 宠物档案模块）、详情（78:3076）、新增/编辑（95:1481 / 95:1844）。本 PR 包含初始页面、接手修复和用户后续授权的底栏统一。

详情/表单原先缺少公共样式而错位，底栏与性别按钮重叠，预览保存不能跨页同步；这些问题已修复。姓名、品种、体重、日期、年龄、备注使用真实组件与数据；头像优先使用 avatarUrl，其他宠物不套用豆豆的记录。长名字、备注、多只宠物及新增后重复保存已覆盖。

底栏现在由 `ConsumerBottomNavigation` 和 `ConsumerPageLayout` 统一维护，宠物列表、详情、表单、编辑资料四个页面复用。列表独立入口补上底栏；图标、菜单、样式、安全区、内容留白、键盘监听及保存中禁用统一。表单保留未保存内容的离开确认；未实现的主入口仍提示尚未接入，不伪造业务跳转。

## 分包和范围

- 保留一个小程序工程/AppID、既有主包、宠物普通分包与商家普通分包；不新增虚假的一级 tabBar 路由。
- 轻量导航与五个原始图标由主包共用，业务背景、头像和宠物字体留在原分包。主包 711,734 B，宠物分包 1,818,265 B，商家 7,180 B，均低于 2 MiB 内控线；整包比 f7e6428 减少 11,905 B。
- package-check 增加原图哈希、共享组件依赖方向、导航字节不重复进入分包及业务资源留在分包的检查。
- 用户授权的范围扩展记录在阶段任务及 NAVIGATION-REFACTOR.md。未修改 shared/merchant/backend；用户本地 project.config.json 不入提交。本轮是工程改造，不新增 CCR；既有字段缺口继续 CCR-C002-PET-DISPLAY-001（PROPOSED）。

## 验证与证据

- typecheck、52/52 单测、微信构建、包体检查及架构源码检查通过。
- 微信开发者工具 2.02.2608060 / 基础库 3.17.2 / iPhone 12/13 (Pro) 390px：8 项公共导航专项，另有 14 项宠物业务回归。专项覆盖四页相同底栏、安全区、五列、当前栏目、未实现入口提示、离开确认及保存按钮无遮挡；弹窗结果使用明确 mock。
- 当前截图与结果在 `planning/issues/wave-2/C-002-pet-page/evidence/navigation/`，业务回归在其 regression 子目录；来源与哈希见 navigation-validation.json。
- 接手时已修正截图脚本的滚动残留与 414→402 整窗压缩问题。此前 evidence/takeover 的 390/414 对照属于历史提交，不代表本轮最新底栏。最新 CI 以本 PR head 的运行结果为准。

## 待验收与边界

- 仍是显式内存预览，真实 HTTP/数据库、头像上传、宠物类型编辑、疫苗/驱虫添加未接入。没有新增 HTTP DTO/Schema/Event。
- Figma 在线工具未注入本会话，使用已导出的官方整帧/节点规格/原始素材（版本 2397539525915641008）。视觉仍待用户复核，完整 C-002 非 DONE，不自行合并。
- 本轮没有新的 414px 或物理真机验证。DevTools 不支持 mock onKeyboardHeightChange，键盘显隐的物理验证明确未执行，不把源码监听当成测试通过。
