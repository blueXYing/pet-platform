# M-002 服务项目管理页 — 实施方案（PLAN v1）

日期：2026-09-22。角色：Merchant Frontend（M-002）＋ C-End 协作。
状态更新（2026-09-22 实施轮）：§5 六项裁决建议已获用户裁决（设计源=10:5255/11:5768、直接售卖/套餐包含裁剪、履约按契约两值、统一字典、状态机按写入方契约 DRAFT/REVIEWING/ACTIVE/OFFLINE/REJECTED、标题"服务管理"+缺失字段补齐）；服务写入契约 v0.2 已原则批准（角色A实现中），**本 PLAN 切片 B/C/D 已实施**——交付见本目录 [HANDOFF.md](HANDOFF.md) 与 [NAVIGATION-BASIS.md](NAVIGATION-BASIS.md)，差异登记见 ../C-003-design-inputs/INVENTORY.md §6。原文留档如下。

## 1. 门禁与依赖

| 依赖 | 状态 | 结论 |
|---|---|---|
| 商家端 Figma 设计源 | **已核验**（本轮角色C集中抓取，版本 `2401168563353004923` 与登记表一致） | 节点 `10:5255`（商品管理列表）、`11:5768`（添加商品表单），截图/结构摘要见 `../C-003-design-inputs/` |
| C 端服务读契约 | 已冻结（CCR-W2-API-001 SVC-D1..D5） | 仅约束读侧投影；对 M-002 页面无直接约束，但保证"商家发布→C 端可见"的下游语义 |
| **商家端服务写入契约**（HTTP10 §4.10 服务项目管理路由族） | **未冻结**：服务写入方登记于 ADM-001"服务操作"范围（SVC-001 §7 承接表），需另走 CCR | **本 PLAN 不含实现**；Mock 不冒充真实联调 |
| `service_item` Schema（06 号 §1） | 已定 | status `DRAFT/ACTIVE/OFFLINE`、price DECIMAL(18,2)、duration_minutes、fulfillment_type、version；PRD §6.3 的 待审核/审核驳回/售罄 状态超出已批 Schema（见 §5 差异 5） |

## 2. 设计源与"商品管理"frame 裁决

设计端 key `Usvn3d6UCVCAlDxou5KAK8`；本轮采用版本 `2401168563353004923`（与 20 号登记表一致，未变化）。

**裁决建议（需人工确认）**：`10:5255`/`11:5768`「首页-商品管理（-添加商品）」**就是 M-002 服务项目管理页的设计源**。依据与字段级差异清单见 `../C-003-design-inputs/INVENTORY.md` §4，要点：
- 列表 8 项全部为服务（洗澡/美容/疫苗/绝育/寄养/体检/上门喂养/训练），无实物商品；V1 不做套餐/次卡/普通商品，商家端亦无其他服务管理 frame；
- 表单字段与商家 PRD §5.5 字段表逐项对应（商品名称↔服务名称、价格↔销售价格、划线价、分类、适用对象↔适用宠物类型、履约方式、服务时长(分钟)、是否需要核销、套餐上架开关↔状态、头图↔封面图、商品描述↔服务说明）；
- 需按 V1 修正的差异：①售卖模式"直接售卖"应裁剪（V1 固定单次预约服务）；②"套餐包含项目"不在 PRD 字段表；③履约方式三选项 vs 契约 `IN_STORE|PICKUP_DELIVERY`（"只送不接"无契约）；④商品分类 pills（洗护/美容/医疗/寄养/其他）vs C 端统一服务字典 11 类；⑤PRD §6.3 待审核/审核驳回/售罄状态超出已批 Schema；⑥TabBar 差异同 C 端 D1。

## 3. 页面规划（获批后实施）

### 3.1 frame 映射

| 页面 | 设计源 | 路由（规划） | 状态 |
|---|---|---|---|
| 服务项目管理列表 | `10:5255`（402×898） | `merchant/pages/services/index`（merchant 分包新增页） | 待写入契约批准 |
| 服务新增/编辑表单 | `11:5768`（402×1611） | `merchant/pages/services/edit` | 同上 |

列表结构（局部坐标，`nodes/frame-summaries.json` mer `10:5255`）：
- 顶部：状态栏区 0–86（#FFF6E5）+ 返回箭头 + 标题"商品管理"（→确认后按裁决改"服务管理"或保留设计原文，**默认按设计原文**，更名需人工确认）；
- 计数行 y≈99："共 8 个商品"（15px #6B7280）＋右侧"添加商品"（15px/600 #5BAAE8＋加号 9×9）；
- 服务卡 368×88 r17 白底描边 #C0ECFF：名称 15px/600 #111827、价格 17px/700 #FF7A00、右上圆形操作钮 47×26（#5BAAE8=上架中 / #D1D5DB=已下架），行距 ~12.9；
- 底部 TabBar（同 §5 差异6）。

表单结构（`11:5768`）：头图（374×170＋"更换头图"角标）、商品名称/价格（元）/划线价（可选）输入（#F9FAFB r11.6 描边 #E5E7EB）、商品分类 pills、适用对象 pills、售卖模式、履约方式、套餐包含项目 pills、服务时长(分钟)、是否需要核销、商品描述/套餐详情 textarea（0/500）、套餐上架开关（"开启后客户可在线购买此套餐"）、"保存商品"按钮（#5BAAE8 r11.6）。

### 3.2 目录与组件（遵循 C/M 分目录与最小公共改动）

```text
frontend-miniapp/src/merchant/
  services/
    model.ts          # 视图状态 + 严格解码器 + 设计样例 + 契约 Mock（获批后）
  pages/services/
    index.tsx / index.config.ts / page.css
    edit.tsx / edit.config.ts / edit.css
    assets/           # 10:5255/11:5768 节点级切图（届时从 C-003-design-inputs 追加导出）+ manifest.json
```
- 共享层（`src/shared/merchant-repositories.ts` 路径白名单、`app.config.ts` 分包注册）按 MS-3 先例由 C-End 作为唯一 Writer 落地，M-002 在 PLAN/交接登记后提请；
- 商家端 TabBar：工作台既有页面语言（MS-3 无原稿先例）或复用商家工作区导航——设计稿 TabBar 为草稿导航（差异6），不据此重排。

### 3.3 Mock 策略（与 C-003 同一模式）

- `PreviewXxxRepository`：无网络、无会话、无持久化；响应严格匹配**届时已批**的写入契约形状（ID/金额 String、写操作 requestId 幂等、服务端 status/actions 不重算、404/503 两分不混同）；
- 设计样例（销量、审核标签等无契约字段）按 `designSamples` 先例标注"仅视觉"，登记契约缺口；
- 页面 `preview=1` 走 Mock；真实模式仅接已实现后端，未接通时显式失败关闭，不冒充联调；
- 切图：从 `../C-003-design-inputs/assets/` 已导出资产起步，缺项按 FETCH-LOG 方式补导（节点级、字节原样、manifest 登记）。

## 4. 实施切片（依赖序，待批后启动）

1. **A｜写入契约 CCR**（Owner=ADM-001/MER-001 名义，M-002 提供页面字段评审输入）：商家端服务 CRUD 路由族 + 字段/校验/错误 + 状态机对齐（§5 差异③④⑤一并裁决）；验收=人工批准并同步 HTTP10/11。
2. **B｜列表页**（前置 A）：计数行＋服务卡列表（名称/价格/上下架状态钮）+ 添加入口；Mock 契约形状；`10:5255` 一比一。
3. **C｜新增/编辑表单**（前置 A、B）：按 PRD §5.5 修正后的字段集（差异①②裁剪与否按裁决）；requestId 幂等与防重；二次确认（下架/删除）。
4. **D｜测试与验收**：Mock UI + contract 单测（注入 transport）；typecheck/build:weapp/check:package；真机截图/叠图（VIS-001~004）另列，不提前声明通过。

## 5. 待人工裁决清单（不自行决定）

1. "商品管理/添加商品" frame 是否即服务管理设计源（**推荐：是**，依据 §2）；
2. 售卖模式"直接售卖"与"套餐包含项目"是否裁剪（**推荐：裁剪**，V1 只有单次预约服务）；
3. 履约方式取契约两值（到店型/上门接送型）还是扩契约支持"只送不接"（**推荐：按契约两值**）；
4. 商品分类与 C 端统一服务字典 11 类的对齐口径（**推荐：统一字典**）；
5. PRD §6.3 待审核/审核驳回/售罄 vs 已批 Schema `DRAFT/ACTIVE/OFFLINE`：写入方 CCR 内对齐（本 PLAN 不扩状态）；
6. 页面标题沿用设计原文"商品管理"还是改"服务管理"（**推荐：实现用"服务管理"、按无原稿差异登记**，因 PRD 术语为服务项目；或按原稿一比一保留原文，二者取一）。
