# MER-001 S6 申请页面交接

2026-09-20，基线PR54 `5f863b9`。已按用户要求通过Figma插件读取线上设计，使用GPT-6 Astra medium实施，Luna xhigh独立检查请求/身份边界。页面位于 `consumer/pages/merchant-application/index`，独立普通分包；工程入口新增真实页与明确标注的交互预览入口。未实现独立签约页，在线设计中未找到对应节点。

## 页面与交互

申请表单、六类选择、必填校验、医院许可证条件显示、单一底栏、草稿/提交操作与错误提示已实现。五个原始Figma图标及PNG派生的来源与哈希在页面assets/manifest.json中。原稿外的社区卡片不渲染。业务字典/必填字段适配沿用已有批准规则。

实际默认依赖为不可用实现：没有猜造上传/城市/地图接口，也没有提交虚构assetId或申请成功；预览模式只能交互，不能写业务数据。真实页未登录时禁用编辑及提交。内部状态支持审核中/已通过只读、补正后编辑，但当前没有真实HTTP联调证据。

## 验证

- npm test：84/84，0失败/跳过；typecheck、build:weapp、check:package通过。
- 包体静态清单：主包743430 bytes；新申请分包40069 bytes；总计2605642 bytes。不是上传/真机验收。
- 微信开发者工具2.02.2608060、基础库3.17.2、iPhone 12/13(Pro)模拟器、390×844、DPR3。
- 实际工具操作：填写商家名称后读取值一致；展开六类→选择宠物医院→许可证出现；上传/城市/定位均明确不可用；预览保存明确不写业务；空白必填提交出现逐字段错误；滚动底部控件无导航遮挡。未登录页面显示登录提示和不可用控件。
- simulator_screenshot已取首屏、类型展开、医院/校验底部及未登录态；本机证据在 `D:/Temp/mer001-s6-delivery.png`、`mer001-s6-final-options.png`、`mer001-s6-final-bottom.png`、`mer001-s6-real-unauthenticated.png`。截图工具实际输出235×507，不能冒充402原尺寸像素叠图。
- 控制台 `grep -i error` 无匹配。构建过程中工具重载导致过一次找不到元素/超时，构建后重新导航并完成操作；未登录disabled属性单独查询曾超时，仅以截图和组件状态作证，不把该查询写成通过。

## 修复与保留事项

修复参考画布402px被Taro转成rpx导致半宽；参考宽度改为inline CSS。修复异参在途保存被静默合并、同页提交未知结果先另存新版本导致无法重试、城市选择在写入锁定时仍更新显示，以及窗口尺寸变化未更新。未知结果保持原saved revision、锁住输入并允许重试原操作，返回按钮也提示先确认结果。

尚未启用真实写链路：强制重启/页面回收后，需要从ConsumerApi持久化journal恢复UI待决意图，属于接通HTTP前的P1门禁。不得仅替换unavailableDependencies就宣称上线。真实私有上传/定位/城市/HTTP、状态端到端、协议页、通知跳转、微信真机仍待完成。

VIS：当前字体使用系统fallback，尚未补齐原稿Roboto/Inter/Outfit的授权与字符覆盖；系统状态栏/胶囊、功能补充项及截图缩放均已披露，不能声称像素级VIS全通过。设计读取证据见[DESIGN.md](DESIGN.md)。

本轮本地模拟器临时使用主目录已有AppID，结束前恢复worktree原配置；主目录用户配置未修改。GitHub网络当前反复EOF/连接中断，远端状态与推送结果以最终操作为准，不推测PR53/54已合并。
