# MER-001 S6 Figma申请页面实施依据

日期：2026-09-20。用户已安装Figma插件并明确要求继续推进页面，沿用已批准的申请流程、技术契约及GPT-6 medium前端配置。

## 在线设计核对

已通过Figma插件实际调用get_design_context读取用户端文件 `bp2vpcjjA5vZbHvtKkA8wl`、节点 `132:862`。该节点为[成为商家-填写](https://www.figma.com/design/bp2vpcjjA5vZbHvtKkA8wl?node-id=132-862)，当前尺寸402×1142；类型展开为[132:1170](https://www.figma.com/design/bp2vpcjjA5vZbHvtKkA8wl?node-id=132-1170)。当前底部导航位于699系列节点，旧归档402×1124不能当本次线上尺寸证据。

同时读取两个设计文件Page 1的在线metadata定位节点。用户端只有填写/类型展开稿可用于本轮入驻；商家端 `Usvn3d6UCVCAlDxou5KAK8` 未定位独立协议签署/申请审核结果画面，“审核中”相关文字属于退款申诉，不拿它代替入驻状态稿。未修改Figma源文件。

## 本轮实现范围

- 申请表单及类型展开交互、复用现有ConsumerPageLayout和唯一底部导航、注册可达路由。
- 采用线上导出的返回/下拉/定位/拍照/证件图标；原始SVG与PNG派生来源可追踪，PNG由sharp对原SVG直接栅格化，不手画图标。画布外无关社区卡片不进入页面。
- 商家类型直接采用已批准六类字典，门店照片1～6张必填，医院类显示行业许可证字段；这是批准规则对视觉文案/字段的必要适配。
- 草稿、提交、状态只使用现有契约；缺少实际HTTP、私有上传、城市/地图Provider时显示不可用，不伪造已上传ID或审核成功。
- 视觉预览使用独立显式模式，与真实身份和写接口隔离；预览交互不能记为真实后端联调。

## 验证环境

微信工具实际路径为 `D:/soft/微信web开发者工具/wechatide.cmd`，版本2.02.2608060，技能0.3.9匹配，登录有效、tokenRequired=false。首次按其他目录定位的失败已由真实进程安装路径纠正。独立worktree仅为本地模拟器暂用主目录现有AppID，提交前恢复原配置；不改主目录用户配置。

模拟器验证目标：页面加载→填写→展开六类选择→选择宠物医院显示必需许可证→上传/城市/定位缺依赖给出明确反馈→空白提交逐字段提示→底栏与键盘不遮挡。截图、真实交互结果和剩余差异在本轮交接中记录，构建通过不代表微信真机或全链路验收。

基线5f863b9（PR54）。本轮开始时远端GitHub连接EOF，未获新的合入事实，不能把PR53/54写成已合并；本地在 `codex/mer001-pages-20260920` 独立分支实施。
