# 编辑资料页：具体实现规范

来源：`bp2vpcjjA5vZbHvtKkA8wl / 127:1652 / 2397539525915641008`。所有尺寸为 402px 原稿坐标，画布高 812px。

这份文档把逐节点 JSON 整理为可读的实现要求。完整元素及切图链接见 [本页清单](README.md)，精确测量见 [spec.json](spec.json)。本页尚未做微信 VIS 验收。

## 字体

| 用途 | 原稿字体 | 字号 / 字重 | 行高 | 颜色 |
|---|---|---|---|---|
| 顶部“编辑资料” | Outfit | 16px / 700 | 24px | #1D0D07 |
| 头像/昵称/性别/手机号/签名标签 | Roboto | 14px / 400 | 20px | #402E26 |
| 昵称输入值 | Roboto | 14px / 400 | 20px，右对齐 | #1D0D07 |
| “保存”按钮文字 | Roboto | 14px / 500 | 20px，居中 | #E4F6F3 |
| 签名字数提示 | Roboto | 11px / 400 | 16.5px，右对齐 | #53BFEF |
| 底栏文字 | Roboto | 10px / 500 | 15px | 逐节点 fills |

以上为 Figma 声明值。中文回退字体未冻结；先核验目标机型实际字形与基线，不能将西文字体名称直接当作中文已匹配的证明。

## 已测量的布局

- 标题文字边界：`x=59, y=12, width=64, height=24`，使用真实文字。
- 表单左侧标签 x=43；“头像”y=97、“昵称”y=177、“性别”y=241、“手机号”y=305、“个性签名”y=369.5，均按节点父容器定位。
- 昵称输入区域：`x=83, y=177, width=277, height=20`；文本右对齐，按原稿截断属性处理。
- 保存按钮：`x=29, y=532.5, width=343, height=44`，圆角12，填充 #53BFEF，文字居中。
- 签名字数提示区域：`x=330, y=481.75, width=30, height=17`；“10/60”只是原稿示例，实际字数与上限必须由批准契约确定。
- 页内坐标用于还原测量，不能把整个页面实现成无响应的绝对定位截图。表单、按钮、输入、底栏分别作为真实组件，按父容器布局与目标窗口复核。

## 本页切图

本次识别 8 个独立图形资产引用：头像、返回箭头、头像行右箭头，以及底栏 5 个图形（含中间组合图形）。

- 头像节点 `127:1897`：保留原图和原稿裁剪的2× PNG，生产使用用户头像。
- 返回箭头节点 `127:1884`、右箭头节点 `127:1898`：使用对应原图形导出。
- 底栏 `127:1682 / 127:1690 / 127:1702 / 127:1707 / 127:1715`：分别引用原稿组合图形；不得把图标旁的文字烘焙进图片。
- 表单卡片背景、边框、性别选择胶囊、签名输入框和保存按钮使用代码样式，文字保持可输入/可更新。

## 交互和状态待接入

头像选择/上传、昵称编辑、性别选择、签名编辑、保存及返回依 C-002 的 USR/AUTH 契约实现。加载、键盘、保存中、成功、失败、上传取消/拒绝和会话失效状态仍需补设计/交互确认。设计样例手机号始终脱敏展示；不从图稿推断真实权限或上传规则。
