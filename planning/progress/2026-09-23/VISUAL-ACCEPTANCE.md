# 2026-09-23 用户视觉验收通过与页面验收记录

## 用户结论

**视觉验收通过**（用户 2026-09-23 明示）。验收口径=原稿+已批差异（20 条 VIS-004 登记于 C-003-design-inputs/INVENTORY.md §6）。

## 验收对象与方式

- 载体：微信开发者工具（真实 appid 本地配置）+ `wt-m-svc-pages/frontend-miniapp` Mock 构建（`?preview=1` 契约 Mock，不冒充真实联调）。
- 页面：门店列表（110:480/690:6370）、商家详情（690:6660）、服务详情（690:2025 同稿）、服务管理列表（10:5255）、服务新增/编辑（11:5768）；工作台入口页（MS-3 既有）。参考截图：`planning/issues/wave-2/C-003-design-inputs/acceptance-20260923/01-06*.jpg`。
- 过程由协调者以 wechatide CLI 驱动（导航/元素断言/截图），窗口供用户直接查看。

## 真实 API 窗口级证据（同日，PR#75 修复后）

- 本地真实后端（M002IntegrationServer@18081，真实 MySQL/Redis，测试域桩=登录码/素材/封面签名，与库内验收同缝）。
- 门店列表真实模式：页面自身 `GET /api/v1/c/stores` → 200 → 渲染种子门店（网络日志+元素级断言+视觉确认）；截图 `real-*.jpg` 已入库。
- 门店详情/服务详情真实模式呈现设计登录引导态（DevTools wx.login 真码不被测试桩接受，属既有验收缝隙；页面层登录门控与 STR-D8 的张力已登记为后续裁决项）。
- 共享客户端层全链证据（发布→审核→匿名找店→进店→看到→下架隐藏/404）见 `M-002-service-pages/INTEGRATION-EVIDENCE.md`（PR#72）。

## 修复记录（随 PR#75 合入）

- stores 页匿名目录读曾被 `scope.run()` 登录上下文拦截（NO_CONTEXT 同步抛→零请求失败关闭），与 STR-D8 页面接线相悖；修复为 `runCatalogRead`（有上下文守卫/匿名直读），新增正反测试，163/163 绿。
- 勘误：上轮"常量构建打坏页面模块"为 DevTools 编译缓存假象（需 `debug_clear_cache` 后刷新），非真实缺陷；已在 PR#75 描述披露。

## 未完成与如实边界

- 完整业务流程的**用户人工走查**（模拟器内从商家发布到消费者看到的连续操作）未执行——用户仅做视觉验收；分层证据（模块测试/共享层真实HTTP/窗口级真实渲染）如上，不冒充完整人工 E2E。
- VIS 真机（手机）验证未做。
- 预约/订单/支付不在本轮范围。
