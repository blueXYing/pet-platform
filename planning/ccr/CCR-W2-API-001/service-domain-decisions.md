# SVC-001 服务域决定回执（2026-09-22）

来源：用户在当前任务中明确回复（对 [服务域提案 v0.2](service-domain-proposal.md) 的审阅）：

> 同意 SVC-D1（含 D1b）、D2、D3、D4、D5，允许本切片一并实现 D5，商家模块变更独立提交并披露。补充明确：确认不存在或不具备资格时隐藏、详情返回 404；事实源故障、读取失败或状态未知仍返回 503，不得用 known=false 混同处理。按此同步权威契约、实现、测试并提 PR；完整业务流程未验收的部分继续如实标注。

## 已确认

1. **SVC-D1/D1b**：四条件合取资格与可见性规则；详情不可见一律 404 `SERVICE_NOT_FOUND` 不区分原因。
2. **SVC-D2**：本切片两条服务读路由；门店两条路由由 MER-001 后续切片承接（已登记）。
3. **SVC-D3**：查询时值拷贝、HTTP 金额 String。
4. **SVC-D4**：无写入方如实披露；不得据此声明完整业务验收完成。
5. **SVC-D5**：pet-merchant-api 增补第四查询 `MerchantDisplayEligibilityApi`（无所有者前提、仅供展示、不授予权限）；**本切片一并实现，商家模块变更独立提交并在 PR 披露**。
6. **错误语义细化（覆盖提案 v0.2 的 known 标志设计）**：确认不存在或不具备资格 → 隐藏/404；事实源故障、读取失败或状态未知 → 503；**两者不得混同，撤下 known=false 双标志设计**，改用 NOT_FOUND 与 DEPENDENCY_UNAVAILABLE 的显式区分（与既有所有者视角实现一致）。

## 权威同步（随本切片 PR）

07 号 §4.2/§5.1.1、10 号 §3.3.1、11 号两操作 `ACCEPTED_CONTRACT_NOT_IMPLEMENTED`、27 号 §4 第四查询行。

## 保留边界

- 排期/库存锁/订单创建/支付不提前；ORD008 仅提交快照模块证据。
- 真实 E2E 在服务写入方交付前保留未完成；完整预约流程未验收部分继续如实标注。
- 商家模块文件属 MER 域，按唯一 Writer 规则以独立 commit 交付。
