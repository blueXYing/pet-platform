<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petplatform.id.core.mapper.SnowflakeNodeMapper">

    <!-- snowflake_worker_state statements, SQL kept verbatim (PLAT-002 S2 / PLAT-006). -->

    <update id="setTimeZoneUtc">SET SESSION time_zone = '+00:00'</update>

    <select id="selectByNode" resultType="com.petplatform.id.core.mapper.SnowflakeNodeRowEntity">
        SELECT * FROM snowflake_worker_state WHERE node_id=#{nodeId}
    </select>

    <select id="selectByNodeForUpdate" resultType="com.petplatform.id.core.mapper.SnowflakeNodeRowEntity">
        SELECT * FROM snowflake_worker_state WHERE node_id=#{nodeId} FOR UPDATE
    </select>

    <select id="sampleNow" resultType="java.time.LocalDateTime">
        SELECT NOW(3)
    </select>

    <update id="updateAcquired">
        UPDATE snowflake_worker_state
        SET owner_incarnation=#{owner}, fence=#{fence}, grant_start=#{start}, grant_through=#{through}, reserved_through=#{reservedThrough},
            lease_until=#{leaseUntil}, updated_at=NOW(3)
        WHERE node_id=#{nodeId} AND fence=#{expectedFence} AND reserved_through=#{expectedReservedThrough}
    </update>

    <update id="updateRenewed">
        UPDATE snowflake_worker_state
        SET grant_through=#{through}, reserved_through=#{reservedThrough}, lease_until=#{leaseUntil}, updated_at=NOW(3)
        WHERE node_id=#{nodeId} AND owner_incarnation=#{owner} AND fence=#{fence} AND reserved_through=#{expectedReservedThrough}
    </update>

</mapper>
