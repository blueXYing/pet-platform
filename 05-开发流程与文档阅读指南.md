# 开发流程与文档阅读指南

适用对象：负责产品与合并决策的人工 CTO。用于随时查阅“现在到哪一步、谁负责、该看哪里、下一步做什么”。

本指南说明现有协作流程，不替代 `WORK_EXECUTION_PROTOCOL.md`、`AGENTS.md` 或产品基线。项目当前状态以 `WORK_STATE.md` 为准，不以历史聊天或本指南推测。**阅读本指南不代表批准启动开发、修改契约或合并 PR。**

## 一、先记住分工

**AI／Work 负责：** 检查资料和依赖、规划任务、整理技术方案、协调并行开发、编写代码、运行测试、修复问题、整理 PR 和验收证据。你批准合并后，由 AI 核验并执行合并。

**你负责：** 确认产品目标和范围，提供必要设计或外部对接资料，对明确的产品取舍及重大契约变更作出决定，按具体步骤确认产品体验，批准合并和发布。

你不需要默认自己运行命令、配置分支或阅读全部测试日志。确实需要手动操作时，AI 应说明操作位置、步骤、预期结果，以及为什么无法自动完成。

## 二、通用路径占位符

以下是模板字符串，**不是已经存在的具体文件链接**。使用时由 AI 替换为本次真实路径，并提供可点击的文件链接。

| 占位符 | 含义 |
|---|---|
| `{PROJECT_ROOT}` | 项目根文件夹 |
| `{N}` | 当前研发波次编号 |
| `{ISSUE_ID}` | 当前任务编号 |
| `{CCR_ID}` | 当前技术契约变更记录编号 |
| `{PR_NUMBER}` | 当前 GitHub PR 编号 |
| `{REVIEW_REPORT_PATH}` | 当前真实存在的验收／收尾报告路径，由 AI 明确给出 |

常用模板：

```text
{PROJECT_ROOT}/WORK_STATE.md
{PROJECT_ROOT}/planning/WAVE_{N}_PLAN.md
{PROJECT_ROOT}/planning/READY_QUEUE_WAVE_{N}.md
{PROJECT_ROOT}/planning/issues/wave-{N}/{ISSUE_ID}.md
{PROJECT_ROOT}/planning/ccr/{CCR_ID}.md
{PROJECT_ROOT}/{REVIEW_REPORT_PATH}
```

说明：不同历史波次的文件命名可能不同，不能因模板推定文件存在。AI 应先核实真实文件再指引你阅读。`Wave {N}` 是第几批研发任务；`W0～W4` 是流程阶段，两者不是同一个编号。

## 三、每一步怎么进行

### 第一步：了解当前状态

- **AI 负责：** 读取最新状态、已合并成果、阻断和授权记录，说明目前可以做什么。
- **你负责：** 看当前结论，确认是否继续下一步；无需重读全部历史。
- **必看：** 根目录 `WORK_STATE.md` 的当前阶段、状态、当前结论和待处理事项。
- **需要时再看：** 根目录 `WORK_EXECUTION_PROTOCOL.md`；`planning/` 中对应波次的执行记录和收尾报告。
- **完成后：** 明确接下来是规划、规范、开发、验收，还是合并。

### 第二步：规划一批任务

- **AI 负责：** 基于已有 Issue 检查依赖、范围、角色、文件所有权和测试要求，给出近期可执行队列，不按 PRD 章节机械串行开发。
- **你负责：** 看这一批的目标、承诺范围、条件目标，以及需要你提供的材料。
- **必看文件夹：** `planning/`。
- **必看文档：** `WAVE_{N}_PLAN.md`、`READY_QUEUE_WAVE_{N}.md`。
- **重点看：** 本批做什么、不包括什么、哪些能先开始、哪些依赖未满足、人工事项。
- **完成后：** 规划文档按协议审核合入；若尚未授权启动，再单独确认本波启动范围。已经明确批准过的动作，不重复索要同一批准。

### 第三步：查看即将执行的 Issue

- **AI 负责：** 选取满足条件的任务，解释范围、前置条件和完成标准。
- **你负责：** 确认任务是否多做、漏做，尤其关注产品行为和验收结果。
- **必看文件夹：** `planning/issues/wave-{N}/`。
- **必看文档：** `{ISSUE_ID}.md`。
- **重点看：** `Source／来源`、`Allowed／Forbidden`、`Acceptance Criteria／验收标准`、`Required Tests／测试要求`。
- **完成后：** 满足门禁的任务才进入开发。一个需要改代码的 Issue 对应一个独立分支和工作区、一个短生命周期任务。

### 第四步：补齐技术契约或产品决定

- **AI 负责：** 找出真正缺失或冲突的约定，整理现有规定、具体问题、建议选项及影响。缺契约走 CCR，不能开发时临时猜接口。
- **你负责：** 审查具体可理解的方案；需要产品取舍或重大契约批准时作出决定。无需从空白写技术方案。
- **必看文件夹：** `planning/ccr/`，以及 `planning/` 的未决事项文档。
- **必看文档：** 当前 `{CCR_ID}.md`、`OPEN_DECISIONS.md` 或 `BLOCKED_QUEUE.md` 中被指出的条目。
- **需要时再看：** `docs/00-ssot/`、`docs/01-prd/` 的对应章节，以及 `docs/03-database/`、`docs/04-api/`、`docs/05-events/`、`docs/06-scheduler/` 的相关契约。
- **完成后：** 只有获批且已同步的约定才能供实现使用。“规范草案完成”不等于“整个 Issue 完成”。

### 第五步：并行开发

- **AI 负责：** Backend Core、Transaction Backend、C端、商家端、运营端、QA 按依赖和所有权协作；处理代码、分支、冲突和测试。
- **你负责：** 通常无需操作；需要材料或产品裁决时，按 AI 给出的具体问题回应。
- **按需查看：** 当前 Issue 文档、`planning/READY_QUEUE_WAVE_{N}.md`，以及对应任务的进度说明。
- **代码位置：** `backend/`、`frontend-miniapp/`、`frontend-admin/`。你不需要默认逐文件审代码。
- **完成后：** 输出可审核的代码提交、PR、测试结果和遗留风险。不能为了保持并发而越过依赖或提前实现业务。

### 第六步：技术验证与产品体验验收

- **AI 负责：** 运行构建、单元测试、接口检查、平台验证和必要的集成测试；修复失败，提供准确证据。
- **你负责：** 对已经可体验的产品页面，按明确清单确认流程、文案和效果是否符合预期。若工具确实无法代办某一步，再做指定的手动操作。
- **先看：** AI 提供的 `{REVIEW_REPORT_PATH}`，重点是通过项、失败项、未执行项和风险。
- **需要时再看：** `docs/07-testing/` 中的验收标准；对应工程的 `evidence/` 目录；GitHub PR 的 `Checks`。
- **产品原型验收：** 用户／商家页面按获准 V1 原稿和原始切图一比一还原，查看节点、素材与对照证据；工程示例页面不是产品视觉交付。
- **完成后：** 确认是“工程壳／Mock 通过”，还是“真实接口／完整业务通过”，两者不能混称。

### 第七步：审核与批准合并

- **AI 负责：** 汇总改了什么、为何修改、测试结果、风险及目标分支；必要时先形成集成候选，避免让你重复合并已有实现。
- **你负责：** 阅读指定 PR 的说明和验收结论，提出问题或明确批准合并。
- **必看位置：** GitHub `Pull requests` → AI 指定的 PR → 说明与 `Checks`。需要细查代码时再看 `Files changed`。
- **注意：** `6/6` 表示六项检查通过，不是六个 PR 都要分别合并。看见多个 PR 时，以 AI 明确的合并清单为准。
- **完成后：** AI 重新核对批准的提交、目标分支和 CI，执行合并并核验结果。不要自行把资料基线 PR 合入错误目标。

### 第八步：收尾集成审查

- **AI 负责：** 检查合并后的最终版本、CI、任务状态、跨模块一致性、追踪关系、未决契约与未验收范围；同步状态文档，保留历史证据。
- **你负责：** 看封板结论和遗留事项；需要合并收尾文档时，按指定 PR 批准。
- **必看：** `planning/` 中当前收尾报告、`WORK_STATE.md`、`BLOCKED_QUEUE.md`。
- **完成后：** 本批完成不等于整个产品完成。符合条件后进入下一批规划，不自动启动下一波开发。

### 第九步：准备正式发布

- **AI 负责：** 在全部发布前置满足后整理完整业务验收、生产配置、迁移、权限、监控和回滚方案。
- **你负责：** 单独确认发布范围、时间和生产影响，批准发布。
- **必看：** AI 明确提供的发布计划、验收总结、迁移与回滚文档；这些文件未形成前，不能假定已经就绪。
- **区别：** `develop` 用于集成开发成果；合入它不代表正式上线。发布到 `main` 或生产需要独立授权。

## 四、常见状态怎么看

| 状态或说明 | 含义 | 你需要做什么 |
|---|---|---|
| BLOCKED | 某个必要条件未满足 | 看具体阻断；只有材料或产品决定需要你处理时才操作 |
| SPEC_READY | 明确的规范阶段具备条件 | 不把它当作完整业务实现 READY，先看阶段范围 |
| READY | 任务具备相应执行条件 | 仍核对当前 Wave 是否已获启动授权 |
| IN_PROGRESS | 任务执行中 | 等关键结果；不必自己运行同一套测试 |
| REVIEW | 形成审核候选 | 看 PR 和证据；不等于已合并 |
| DONE | 既定 Issue 范围完成 | 不外推为关联业务、下一波或整个 V1 完成 |
| Draft PR | 草稿候选 | 可以阅读；不是自动合并指令 |
| CI success | 这次指定检查通过 | 核对检查范围，尤其是否有 skipped、Mock 或未执行的真实能力 |

## 五、不明白时先找哪个文件夹

| 你的问题 | 优先查看 |
|---|---|
| 现在到哪一步？ | 根目录 `WORK_STATE.md` |
| 下一批做什么？ | `planning/WAVE_{N}_PLAN.md` |
| 为什么这个任务没开始？ | `planning/READY_QUEUE_WAVE_{N}.md`、`BLOCKED_QUEUE.md` |
| 这个任务到底包含什么？ | `planning/issues/wave-{N}/{ISSUE_ID}.md` |
| 产品规则以什么为准？ | `docs/00-ssot/`，再看 `docs/01-prd/` 及已批准补充 |
| 技术为什么这样选？ | `docs/02-architecture/` 中的现行技术基线 |
| 为什么需要我裁决？ | `planning/OPEN_DECISIONS.md` 或当前 CCR |
| 功能验收怎么判通过？ | `docs/07-testing/` 和当前 Issue 的验收标准 |
| 这次实际测了什么？ | 当前验收报告、Owner 的证据目录、对应 PR 的 Checks |
| 应该合并哪个 PR？ | AI 提供的具体 PR 和合并清单，不能仅看列表猜测 |

## 六、可以直接使用的沟通句式

这些句式只是模板，不是现在自动生效的授权。请先替换占位符，并确保方案已经具体可审核。

```text
请说明当前阶段、你负责什么、我负责什么，并给出本步必看的真实文档链接。

请规划 Wave {N}，保留现有任务体系，不启动开发。

批准将规划 PR #{PR_NUMBER} 合入 develop；本次不启动开发。

批准启动 Wave {N}，先执行 {ISSUE_ID} 的已明确阶段，其余按依赖解锁。

请解释 {CCR_ID} 的现有规定、冲突点、建议方案及影响，先不要修改产品规则。

请给我当前可体验内容的检查步骤、预期结果和验收文档。

批准将 PR #{PR_NUMBER} 合入 develop，不发布 main。

请进行本波收尾集成审查，不启动下一波。
```

## 七、以后每一步的引导格式

AI 应按下面的顺序说明，不让你从一堆技术文件里自己猜下一步：

1. **当前阶段与目标：** 这一步要解决什么。
2. **AI 负责：** 将执行哪些具体工作。
3. **你负责：** 本次需要确认或操作什么；不需要操作就明确说无需操作。
4. **必看文档：** 文件夹、真实文件链接和重点章节，通常先给最相关的1～3份。
5. **完成标准与下一步：** 什么证据算完成，完成后进入哪一步，哪些动作仍未获授权。

需要你批准前，AI 应先准备好具体可审核结果，并解释批准依据。已明确授权的正常执行不重复询问；发现真正产品冲突、重大契约变更或新的合并／发布动作时，再针对具体事项处理。

## 用户确认的沟通偏好：优先讲人话

先用具体场景解释：用户会遇到什么、建议如何处理、对体验有什么影响、现在需要批准哪一句。不要先堆叠CCR内部编号、数据库字段或算法参数让人工猜。技术实现由AI负责，细节放关联文档备查；确需术语时紧跟一句通俗解释。明确区分登录有效期与结果找回窗口、方案接受与代码已完成、测试通过与正式上线。已批准事项不重复询问。


## 2026-09-16 本地目录与资料管理补充

主目录用于查看/运行最新已合并的develop代码；开发修改使用任务分支及独立工作树。拉取前区分“代码未提交”“本机配置/日志”“本地参考素材”，不要一键提交所有文件。当前已合并阶段见[Work State](WORK_STATE.md)，Figma原始资料保管见[设计来源](planning/DESIGN_SOURCES.md)。本地备份/历史分支不作为新的开发待办来源，实际文件与提交核验后再清理。
